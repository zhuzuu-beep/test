[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "henan-recruit-monitor"
version = "0.2.0"
description = "河南省官方招聘公告监控与飞书推送"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
henan-recruit-monitor = "app.cli:main"

[tool.setuptools]
packages = ["app"]
